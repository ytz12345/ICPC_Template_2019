cdq经常解决的问题:
1.三维偏序
  对一维排序,然后cdq处理,处理到某个区间时,我们拿出前一半的插入和后一半的查询
  插入是在(x,y)插入一个点,查询是查询一个二维前缀和
  直接把所有点排序然后树状数组做即可
2.配合按秩合并的并查集维护图的联通性
  具体可参考板子
3.如果第一个问题中的点可删除怎么办
  假设某个点i出现和消失时间分别为st[i]和ed[i]
  那么对于当前处理的操作序列，对于前一半的插入操作i(即某个点出现了)
  如果ed[i]>r,那么就说明在后一半的时间中始终存活，留下
  对于留下的插入操作，考虑对后一半中的询问操作做贡献
  然后对于后一半的删除操作i
  如果st[i]<l,说明它在前一半中的时间中适中存活，留下
  然后对于留下的这些点，考虑对前一半的所有询问做贡献
  然后递归下去即可