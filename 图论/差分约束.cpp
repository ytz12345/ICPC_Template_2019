/*统一化成d[v]<=d[u]+w的形式，然后连接u->v权值为w的边，做最短路即可
 *为防止图不连通，可建立源点s,由s向所有点连权值为0的边即可
 *判定性问题:可行性直接判负环，spfa进队超过n次,n为节点总个数
 *最大最小值问题:列不等式，根据要确定的点决定起点终点即可，dis[ed]=inf即代表两者无约束关系，可取无穷
 *需要注意的问题:
 *最长路最短路根据源点来确定
 *题目中所有约束条件都要转成不等式，继而转成边
 *多组数据清空，队列，cnt,vis,dis等都要清空,有负环情况一定要清空vis!
 *推荐使用deque哦,deque如果T就转stack
 */